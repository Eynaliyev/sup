/*import { ChatroomPage } from "./chatroom";
xdescribe("Chatroom Page", () => {
	beforeEach(() => {
		this.page = new ChatroomPage();
	});
	xit("should have background image", () => {
		expect(this.page.backgroundImage).toBeTruthy();
	});
});
*/
